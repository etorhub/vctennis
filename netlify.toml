[build]
  command = "npx astro db push --remote && node scripts/migrate-apartment-number.js && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20.10.0"

[[plugins]]
  package = "./netlify/plugins/smoke-test"
